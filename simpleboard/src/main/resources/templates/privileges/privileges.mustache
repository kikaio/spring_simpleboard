<!DOCTYPE html>
<html lang="en">
<html>
    {{>partials/head_partial}}
    <body>
        {{>partial/header_partial}}
        <div id="content">
            <p>Welcome to privilege list page</p>
            <table>
            <th>privilege name</th>
            <th>desc</th>
            <th>update</th>
            <th>delete</th>
            {{#privileges}}
            <tr>
            <td><label><a href="/privileges/{{id}}">{{name}}</label></td>
            <td><label>{{desc}}</label></td>
            <td><button type="button" onclick="location.href='/privileges/update?id={{id}}'" >do it!</button></td>
            <td><button type="button" onclick="onDelete({{id}})" >do it!</button></td>
            </tr>
            {{/privileges}}
            </table>
        </div>
        <div>
        <button type="buttom" onclick="location.href='/privileges/create'">create new role!</button><br>
        <button type="buttom" onclick="location.href='/'">go to index!</button>
        </div>
        <script>
        function onDelete(id)
        {
            var http = new XMLHttpRequest(); 
            var url_to = window.location.origin + "/privileges/" + id;
            http.open("DELETE", url_to);
            http.addEventListener("readystatechange", function(){
                if(http.readyState === 4 && http.status === 200)
                {
                    window.location.reload(true);
                }
            });
            http.send();
        }
        </script>
    </body>
    {{>partials/footer_partial}}
</html>