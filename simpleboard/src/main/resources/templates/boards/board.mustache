<!DOCTYPE html>
<html lang="en">
<html>
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>My Awesome Site</title>
    </head>
    <body>
        <header>
            <h1>Welcome to My Awesome Site - Board Detail Page</h1>
        </header>
        <div id="content">
            <input type="hidden" value={{board.id}} name="board_id"/>
            <p>Hello, cur board is {{board.name}}!</p>
            <div>
            <table>
            <tr>
                <th>post id</th>
                <th>post title</th>
                <th>update</th>
                <th>delete</th>
            </tr>
            {{#posts}}
            <tr>
                <td>{{id}}</td>
                <td><a href="/posts/{{id}}?board_id={{board.id}}">{{title}}</a></td>
                <td><button type="button" onclick="location.href='/posts/update?id={{id}}'">do it!</button></td>
                <td><button type="button" onclick="doPostDelete({{id}})">do it!</button></td>
            </tr>
            {{/posts}}
            </table>
            </div>
            <div>
                <button type="button" onclick="location.href='/posts/create?board_id={{board.id}}&board_name={{board.name}}'">create post on this board</button>
                <br>
                <button type="button" onclick="location.href='/boards'">go to board list</button>
            </div>
        </div>
        <script>
        function doPostDelete(num_id)
        {
            var http = new XMLHttpRequest(); 
            var url_to = window.location.origin + "/posts/" + num_id;
            http.open("DELETE", url_to);
            http.addEventListener("readystatechange", function(){
                if(http.readyState === 4 && http.status === 200)
                {
                    window.location.reload(true);
                }
            });
            http.send();
        }
        </script>
    </body>
</html>